<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <!-- เชื่อมต่อ Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        /* ตั้งค่าพื้นฐานของหน้า */
        body, html {
            height: 100%;
            margin: 0;
            overflow: hidden; /* ป้องกันการเลื่อนหน้าจอ */
        }
        /* จัดการวิดีโอพื้นหลัง */
        .bg-video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1; /* วิดีโอต้องอยู่ด้านหลัง */
        }
        /* จัดการฟอร์ม register */
        .register-container {
            background: rgba(255, 255, 255, 0.9); /* โปร่งแสงเล็กน้อย */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            position: relative; 
            z-index: 1; /* ให้ฟอร์มอยู่ด้านหน้า */
            width: 400px; /* กำหนดความกว้างของฟอร์ม */
            margin: auto; /* จัดกลางในแนวนอน */
            top: 50%; 
            transform: translateY(-50%); /* จัดกลางในแนวตั้ง */
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
        }
        label {
            font-weight: bold;
            color: #333;
        }
        input {
            margin-bottom: 15px;
        }
        button {
            width: 100%;
            background-color: #007bff; /* สีของปุ่ม */
            border: none;
        }
        button:hover {
            background-color: #0056b3; /* เปลี่ยนสีเมื่อ hover */
        }
        .footer-text {
            text-align: center;
            margin-top: 20px;
            color: #777;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <video class="bg-video" autoplay loop muted>
        <source src="https://cdn.pixabay.com/video/2023/02/16/150883-799711528_large.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <div class="register-container">
        <h1>Register</h1>
        <form action="/register" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label">Username:</label>
                <input type="text" class="form-control" id="username" name="username" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password:</label>
                <input type="password" class="form-control" id="password" name="password" required>
            </div>
            <button type="submit" class="btn btn-primary">Register</button>
        </form>
        <p class="footer-text">Already have an account? <a href="/login">Login here</a></p>
    </div>

    <!-- เชื่อมต่อกับ Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
